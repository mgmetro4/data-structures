/* binary tree node */
struct TreeNode {
    int val;
    struct TreeNode *left;
    struct TreeNode *right;     
    };


/** Pre-order Traversal
 * Return an array of size *returnSize.
 * Note: The returned array must be malloced, assume caller calls free().
 */
int* pre(struct TreeNode* root, int* returnSize) {
    
}